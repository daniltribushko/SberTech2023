spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sber_tech_db
    username: postgres
    password: 123456654321
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 8002